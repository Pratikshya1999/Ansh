pipeline {
        agent any
        tools {
          maven 'maven'
        }
        stages {
          stage('SCM Checkout') {
          git 'https://github.com/Pratikshya1999/Ansh'
          }
          stage('Compile-Package') {
          sh 'mvn package'
          }
        }
}
